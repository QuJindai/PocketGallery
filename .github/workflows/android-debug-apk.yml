name: Android Debug APK

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  android-debug:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout PocketGallery
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 37
        run: sdkmanager "platforms;android-37.0"

      - name: Verify downstream scripts
        run: |
          bash scripts/verify/test_materialize_upstream.sh
          bash scripts/verify/test_apply_overlay.sh
          bash scripts/verify/test_build_android_debug.sh

      - name: Build debug APK
        run: bash scripts/build/build_android_debug.sh

      - name: Verify APK exists
        run: test -f .work/gallery/Android/src/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: PocketGallery-debug-apk
          path: .work/gallery/Android/src/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14

      - name: Run pristine upstream lint (informational)
        continue-on-error: true
        working-directory: .work/gallery/Android/src
        run: ./gradlew --no-daemon lintDebug

      - name: Upload upstream lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Gallery-upstream-lint-report
          path: |
            .work/gallery/Android/src/app/build/reports/lint-results-debug.html
            .work/gallery/Android/src/app/build/reports/lint-results-debug.txt
          if-no-files-found: ignore
          retention-days: 14
